<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Baseline Material3 theme. Only colorPrimary is set explicitly.
        All other attributes use Material3 defaults to avoid missing-attribute crashes.
    -->
    <style name="Theme.WebViewTranslator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
    </style>
</resources>
